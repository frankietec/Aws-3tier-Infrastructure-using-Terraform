apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-ingress
  namespace: argocd
  annotations:
    cert-manager.io/cluster-issuer: http-01-production
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # DO NOT include rewrite-target annotation here
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - argocd.fkvr.xyz
    secretName: argocd-tls
  rules:
  - host: argocd.fkvr.xyz
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: argocd-server
            port:
              name: http